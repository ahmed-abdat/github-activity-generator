/**
 * Application-wide constants
 */
const CONSTANTS = {
  // Time constants
  HOURS_IN_DAY: 24,
  MINUTES_IN_HOUR: 60,
  SECONDS_IN_MINUTE: 60,

  // Progress tracking
  PROGRESS_INTERVAL: 10, // show progress every 10 commits

  // Configuration
  DEFAULT_CONFIG: "test",
  ALLOW_FUTURE_DATES: false,
};

module.exports = CONSTANTS;
